# Optimization scenarios

Each file is a complete optimization input with one initial state and one or more requested final
states. The runtime needs no separate target file and never loads a ground-truth causal graph.
